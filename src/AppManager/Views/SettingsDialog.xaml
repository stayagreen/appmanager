<Window x:Class="AppManager.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Height="280" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="OpenCode Go API Key" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="用于 AI 智能扫描项目。在 opencode.ai 订阅 Go 后获取。" FontSize="12" Foreground="#888" Margin="0,0,0,10"/>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtApiKey" Height="28"
                     VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="显示" Width="50" Height="28" Margin="6,0,0,0"
                    Click="ToggleVisibility_Click"/>
        </Grid>

        <TextBlock Grid.Row="3" FontSize="11" Foreground="#999" TextWrapping="Wrap">
            不填写则跳过 AI 分析，仅使用 bat 文件直接启动。<LineBreak/>
            你的 key 只存储在本地计算机上。
        </TextBlock>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Width="80" Height="30" Click="Ok_Click"
                    Background="#0078D4" Foreground="White" BorderThickness="0"/>
            <Button Content="取消" Width="80" Height="30" Margin="10,0,0,0"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
