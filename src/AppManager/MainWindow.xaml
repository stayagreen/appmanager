<Window x:Class="AppManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AppManager.ViewModels"
        xmlns:models="clr-namespace:AppManager.Models"
        Title="程序管理器" Height="600" Width="1000"
        MinHeight="400" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Border Grid.Column="0" Background="#F5F5F5">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                    <Button Content="+ 添加" Style="{StaticResource ActionButton}"
                            Command="{Binding AddProgramCommand}"/>
                    <Button Content="扫描" Style="{StaticResource ActionButton}"
                            Background="#5C2D91"
                            Command="{Binding ScanDirectoryCommand}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
                    <Button Content="刷新" Style="{StaticResource ActionButton}"
                            Background="#666"
                            Command="{Binding RefreshCommand}"/>
                    <Button Content="全部启动" Style="{StaticResource ActionButton}"
                            Background="#228B22"
                            Command="{Binding StartAllCommand}"/>
                    <Button Content="设置" Style="{StaticResource ActionButton}"
                            Background="#555"
                            Command="{Binding OpenSettingsCommand}"/>
                </StackPanel>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Programs}"
                         SelectedItem="{Binding SelectedProgram}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ProgramEntry}">
                             <Border Padding="8,6" CornerRadius="4" Margin="0,0,0,2">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Running">
                                                        <Setter Property="Fill" Value="#22BB33"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Stopped">
                                                        <Setter Property="Fill" Value="#DD3333"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                                <Setter Property="Fill" Value="#AAAAAA"/>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                   FontSize="13"/>
                                        <TextBlock Text="{Binding LoginUrl}"
                                                   FontSize="11" Foreground="#666"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxWidth="210"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Stretch"
                      Background="#E0E0E0"/>

        <!-- Right Panel -->
        <Border Grid.Column="2" Background="White">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with status -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <Ellipse Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedProgram.Status}" Value="Running">
                                        <Setter Property="Fill" Value="#22BB33"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedProgram.Status}" Value="Stopped">
                                        <Setter Property="Fill" Value="#DD3333"/>
                                    </DataTrigger>
                                </Style.Triggers>
                                <Setter Property="Fill" Value="#AAAAAA"/>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding SelectedProgram.Name}" FontSize="22"
                               FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock FontSize="14" Foreground="#888" VerticalAlignment="Center"
                               Margin="10,0,0,0">
                        <Run Text="["/>
                        <Run Text="{Binding SelectedProgram.Status}"/>
                        <Run Text="]"/>
                    </TextBlock>
                </StackPanel>

                <!-- Port info -->
                <Border Grid.Row="1" Background="#F8F8F8" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="API 端口:" FontWeight="SemiBold" Foreground="#555"
                                   Margin="0,0,16,6"/>
                        <TextBlock Grid.Column="1" Text="{Binding SelectedProgram.ApiPort}"
                                   FontSize="16" FontWeight="Bold" Foreground="#333"/>

                        <TextBlock Grid.Row="1" Text="Web 端口:" FontWeight="SemiBold" Foreground="#555"
                                   Margin="0,6,16,6"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBox Text="{Binding SelectedProgram.WebPort, UpdateSourceTrigger=LostFocus}"
                                     Width="60" Height="24" FontSize="15" FontWeight="Bold" Foreground="#333"
                                     VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Text="WS 端口:" FontWeight="SemiBold" Foreground="#555"
                                   Margin="0,6,16,6"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBox Text="{Binding SelectedProgram.WsPort, UpdateSourceTrigger=LostFocus}"
                                     Width="60" Height="24" FontSize="15" FontWeight="Bold" Foreground="#333"
                                     VerticalContentAlignment="Center"/>
                            <Button Content="保存端口" FontSize="11"
                                    Height="24" MinWidth="60"
                                    Margin="10,0,0,0"
                                    Background="#0078D4" Foreground="White"
                                    BorderThickness="0" Cursor="Hand"
                                    Command="{Binding SavePortsCommand}"
                                    CommandParameter="{Binding SelectedProgram}"/>
                        </StackPanel>

                        <TextBlock Grid.Row="3" Text="登录地址:" FontWeight="SemiBold" Foreground="#555"
                                   Margin="0,6,16,0"/>
                        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="{Binding SelectedProgram.LoginUrl}"
                                       FontSize="14" Foreground="#0078D4"
                                       TextDecorations="Underline" VerticalAlignment="Center"/>
                            <Button Content="复制" FontSize="11"
                                    Height="22" MinWidth="44"
                                    Margin="10,0,0,0"
                                    Background="Transparent" BorderThickness="1"
                                    BorderBrush="#0078D4" Foreground="#0078D4"
                                    Cursor="Hand"
                                    Command="{Binding CopyUrlCommand}"
                                    CommandParameter="{Binding SelectedProgram}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Directory -->
                <Border Grid.Row="2" Background="#F8F8F8" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="项目目录:" FontWeight="SemiBold" Foreground="#555"
                                   Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedProgram.Directory}"
                                   FontSize="13" Foreground="#666"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" MaxWidth="550"/>
                    </StackPanel>
                </Border>

                <!-- Port status -->
                <StackPanel Grid.Row="3" Margin="16,10,0,4">
                    <TextBlock Text="{Binding PortStatusText}" FontSize="13"/>
                </StackPanel>

                <!-- Batch files -->
                <StackPanel Grid.Row="5" Margin="16,0,0,12">
                    <TextBlock FontSize="12" Foreground="#999" Margin="0,4,0,2">
                        <Run Text="Start: "/>
                        <Run Text="{Binding SelectedProgram.StartBat}"/>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="#999" Margin="0,2,0,2">
                        <Run Text="Stop:  "/>
                        <Run Text="{Binding SelectedProgram.StopBat}"/>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="#999">
                        <Run Text="Restart: "/>
                        <Run Text="{Binding SelectedProgram.RestartBat}"/>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="#0078D4">
                        <Run Text="AI:    "/>
                        <Run Text="{Binding SelectedProgram.StartCommand}"/>
                    </TextBlock>
                </StackPanel>

                <!-- Output log -->
                <Border Grid.Row="6" Margin="16,8,0,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="运行日志" FontSize="12" FontWeight="Bold" Foreground="#999"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="1"
                                 Text="{Binding SelectedProgram.LogOutput}"
                                 IsReadOnly="True"
                                 Background="#1E1E1E"
                                 Foreground="#D4D4D4"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 TextWrapping="NoWrap"
                                 AcceptsReturn="True"
                                 BorderThickness="0"/>
                    </Grid>
                </Border>

                <!-- Action buttons -->
                <StackPanel Grid.Row="7" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="启动" Style="{StaticResource ActionButton}"
                            Background="#228B22"
                            Command="{Binding StartProgramCommand}"
                            CommandParameter="{Binding SelectedProgram}"
                            IsEnabled="{Binding SelectedProgram.IsStopped}"/>
                    <Button Content="停止" Style="{StaticResource ActionButton}"
                            Background="#DC143C"
                            Command="{Binding StopProgramCommand}"
                            CommandParameter="{Binding SelectedProgram}"
                            IsEnabled="{Binding SelectedProgram.IsRunning}"/>
                    <Button Content="重启" Style="{StaticResource ActionButton}"
                            Background="#FF8C00"
                            Command="{Binding RestartProgramCommand}"
                            CommandParameter="{Binding SelectedProgram}"/>
                    <Button Content="编辑" Style="{StaticResource ActionButton}"
                            Background="#666"
                            Command="{Binding EditProgramCommand}"
                            CommandParameter="{Binding SelectedProgram}"/>
                    <Button Content="删除" Style="{StaticResource ActionButton}"
                            Background="#999"
                            Command="{Binding DeleteProgramCommand}"
                            CommandParameter="{Binding SelectedProgram}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Full-screen loading overlay -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="1"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisible}}"
                Background="#AA000000" Panel.ZIndex="1000">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="{Binding BusyText}" FontSize="18" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <ProgressBar Width="400" Height="8" IsIndeterminate="True"
                             Foreground="#0078D4" Background="#33FFFFFF" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
